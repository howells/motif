> Superseded by the look and mood creative direction (MOT-43). Kept as a historical record.

# Surface Audit: Motif

Date: 2026-05-21

## Detected Stack

- TypeScript pnpm/turbo monorepo. Root scripts cover build, lint, typecheck, tests, and `pnpm check` in `package.json:4-16`; package manager is `pnpm@10.23.0` in `package.json:36`.
- Public package surfaces: `@howells/motif-cli`, `@howells/motif-sdk`, `@howells/motif-mcp`, and deprecated compatibility wrapper `@howells/motif-server`.
- Runtime surfaces: Node 22 CLI, Node SDK using platform `fetch`, local stdio MCP server, terminal Ink Studio.
- Agent surfaces present: CLI structured output, stdin JSON, `--dry-run`, `--describe`, field masks, MCP tools/resources, package smoke tests, CLI contract tests, root `AGENTS.md`, `CLAUDE.md`, `llms.txt`, `.mcp.json`, and package-scoped `apps/cli/AGENTS.md`.
- Recent change: creative direction is now a shared SDK taxonomy and prompt enrichment layer, exposed through CLI schema, CLI dry-run outputs, Series commands, and MCP `generate`/`vary` schemas.
- No HTTP API, hosted web app, public OpenAPI spec, remote MCP transport, retrieval/RAG corpus, or multi-agent runtime in this public repo.

## Scorecard

```text
==============================================================================
                           SURFACE SCORECARD
                           Motif
                           2026-05-21
==============================================================================

  1. API Surface          [---]  N/A   No HTTP API surface
  2. CLI Design           [##.]  2/3   Strong JSON/schema/dry-run surface; exit codes are not semantic
  3. MCP Server           [###]  3/3   Stdio MCP with tools, schemas, resources, structured errors, and tests
  4. Discovery & AEO      [##.]  2/3   README, root AGENTS.md, llms.txt, package docs, and local .mcp.json
  5. Authentication       [##.]  2/3   FAL_KEY/envy config plus documented MCP, history, and ephemeral boundaries
  6. Error Handling       [##.]  2/3   Structured CLI errors with doc_uri/suggestions; no trace_id or semantic exit codes
  7. Tool Design          [##.]  2/3   Typed SDK/CLI/MCP schemas with improved parity; no model-output optimization layer
  8. Context Files        [###]  3/3   Root AGENTS.md, CLAUDE.md overlay, package AGENTS.md, and package READMEs
  9. Multi-Agent          [---]  N/A   Not an agent orchestration system
  10. Testing             [##.]  2/3   CLI/MCP/package tests and CI; no agent eval dataset or regression metrics
  11. Data Retrievability [---]  N/A   No retrievable knowledge/RAG surface

==============================================================================
  TOTAL: 18/24 (scaled: 23/30)
  RATING: Agent-first

  Human-only        Agent-tolerant      Agent-ready        Agent-first
  0          7      8           14      15        22       23        30
==============================================================================
```

## Delta Since 2026-05-20

- Score remains **18/24, scaled 23/30, Agent-first**.
- Tool Design is stronger inside the same 2/3 band: creative direction now has one SDK-owned taxonomy, CLI enum descriptions, CLI/Series dry-run metadata, and MCP schema parity.
- Remaining score blockers are unchanged: semantic CLI exit codes, trace/instance correlation on errors, hosted/full discovery only if Motif gets a docs site, and labeled agent-task eval fixtures.

## Evidence

- CLI package exposes the `motif` binary and agent-first metadata in `apps/cli/package.json:2-8` and `apps/cli/package.json:28-35`.
- MCP package exposes `motif-mcp` and depends on `@modelcontextprotocol/sdk` in `packages/motif-mcp/package.json:13-15` and `packages/motif-mcp/package.json:38-40`.
- Root context lists commands, package map, public surfaces, environment, architecture rules, permission boundaries, and verification expectations in `AGENTS.md:5-83`.
- `CLAUDE.md` is a thin tool-specific overlay that points back to `AGENTS.md` in `CLAUDE.md:3-10`.
- `llms.txt` provides a compact agent-readable index with start-here links, package links, agent interfaces, and tests in `llms.txt:1-33`.
- Local MCP discovery is configured through `.mcp.json:1-10`.
- Security docs explain `FAL_KEY`, local history exposure, MCP trust boundaries, and live fal credit spend in `docs/security.md:3-34`.
- CLI output supports human/JSON/NDJSON modes, field masks, structured JSON emission, and structured stderr errors in `apps/cli/src/utils/output.ts:12-24`, `apps/cli/src/utils/output.ts:78-104`, and `apps/cli/src/utils/output.ts:106-195`.
- CLI input hardening strips control characters and rejects traversal, encoded traversal, embedded query params, unsafe output paths, and missing/unsupported edit paths in `apps/cli/src/utils/input.ts:12-58` and `apps/cli/src/utils/input.ts:109-184`.
- CLI schema introspection exposes creative direction fields from the SDK taxonomy in `apps/cli/src/commands/describe.ts:32-53` and adds them to generate schema properties in `apps/cli/src/commands/describe.ts:225-231`.
- SDK creative direction has typed fields, typed invalid-option errors, canonical ordering, sanitization, and shared enrichment in `packages/motif-sdk/src/creative.ts:4-184`.
- SDK generation enriches prompts before fal request body construction without leaking `creative` as a fal parameter in `packages/motif-sdk/src/generate.ts:226-270`.
- MCP resources expose model, tool, leaderboard, and history-schema data without exposing local history values as a resource in `packages/motif-mcp/src/create-server.ts:122-174`.
- MCP recoverable failures use structured `isError: true` tool results with retryability and suggestions in `packages/motif-mcp/src/create-server.ts:177-198`.
- MCP `generate` has agent-oriented descriptions, annotations, output schema, SDK-backed creative schema, and structured content in `packages/motif-mcp/src/create-server.ts:233-329`.
- MCP `vary` exposes the same creative schema for prompt-guided edits in `packages/motif-mcp/src/create-server.ts:420-470`.
- MCP handlers pass creative direction through to the SDK for `generate` and `vary` in `packages/motif-mcp/src/create-server.ts:651-697` and `packages/motif-mcp/src/create-server.ts:779-800`.
- MCP protocol tests verify tool listing, annotations, output schemas, creative schemas, resources, structured success, structured errors, and creative pass-through in `packages/motif-mcp/tests/tools.test.ts:97-220`, `packages/motif-mcp/tests/tools.test.ts:230-414`, and `packages/motif-mcp/tests/tools.test.ts:482-541`.
- CLI contract tests verify schema introspection, creative dry-run metadata, Series creative enrichment, stdin JSON, structured errors, and utility tools in `apps/cli/tests/cli-contract.test.ts:86-179`, `apps/cli/tests/cli-contract.test.ts:181-318`, and later tests in the same file.
- SDK tests verify creative prompt enrichment, canonical ordering, full initial option coverage, and structured invalid-option errors in `packages/motif-sdk/tests/creative.test.ts:4-105`.
- CI runs install, typecheck, lint, and tests on push and pull request in `.github/workflows/ci.yml:24-28`.

## Findings

### 1. Creative Direction Is Now A Shared Agent Surface

The imported photography taxonomy has been converted into a reusable, typed SDK-owned contract and exposed through the major agent entrypoints.

| Severity | File | Status | Dimension |
| --- | --- | --- | --- |
| Resolved | `packages/motif-sdk/src/creative.ts` | SDK owns creative fields, options, clauses, prompt enrichment, and structured invalid-option errors. | Tool Design |
| Resolved | `apps/cli/src/commands/describe.ts` | `motif --describe generate --format json` exposes creative enum ids with labels, descriptions, and clause previews. | CLI Design, Tool Design |
| Resolved | `packages/motif-mcp/src/create-server.ts` | MCP `generate` and `vary` expose and pass through the same creative object. | MCP Server, Tool Design |
| Resolved | `apps/cli/tests/cli-contract.test.ts`, `packages/motif-mcp/tests/tools.test.ts` | Contract tests cover creative schema, dry-run metadata, Series prompt enrichment, and MCP pass-through. | Testing |

Suggested approach: keep adding taxonomy entries in the SDK only, then let CLI/MCP schemas render from that source.

Dependencies: none.

### 2. CLI Error Semantics Still Stop Short Of Full Autonomy

The CLI emits useful structured error objects, but process-level semantics and trace correlation are still not rich enough for fully autonomous recovery.

| Severity | File | Issue | Dimension |
| --- | --- | --- | --- |
| Medium | `apps/cli/src/utils/errors.ts`, `apps/cli/src/utils/output.ts` | Structured error body includes status/doc_uri/suggestions, but process exit codes are not semantic. | CLI Design, Error Handling |
| Medium | `apps/cli/src/utils/output.ts` | No `trace_id` or `instance` field for correlating failed CLI runs. | Error Handling |
| Low | `apps/cli/src/commands/describe.ts` | Error docs are local schema entries, not stable markdown pages. | Error Handling, Discovery |

Suggested approach: map error catalog metadata to sysexits-style exit codes, add generated `trace_id`/`instance` to structured errors, and document error code pages under `docs/errors/`.

Dependencies: decide whether changing exit codes is a breaking CLI contract.

### 3. MCP Is Agent-First For Local Stdio, Not A Remote Protected Server

The local stdio MCP surface is strong, but there is no remote transport, OAuth protected-resource metadata, or long-running task/progress model.

| Severity | File | Issue | Dimension |
| --- | --- | --- | --- |
| Resolved | `packages/motif-mcp/src/create-server.ts` | Tools have descriptions, annotations, output schemas, structured content, resources, and structured recoverable errors. | MCP Server |
| Low | `packages/motif-mcp/src/index.ts` | Startup failure for missing `FAL_KEY` is a plain fatal message. | Authentication, Error Handling |
| Low | repo root | No Streamable HTTP transport or RFC 9728 metadata, which is acceptable for local-only stdio but would be required if hosted. | MCP Server, Authentication |

Suggested approach: keep the local stdio boundary explicit. Only add HTTP/OAuth metadata if Motif becomes a hosted MCP server.

Dependencies: future hosting decision.

### 4. Discovery Is Good For A Public Repo, Not A Full Web Surface

Motif has strong repo/package discovery, but not the hosted web signals expected from a documentation site.

| Severity | File | Issue | Dimension |
| --- | --- | --- | --- |
| Resolved | `AGENTS.md`, `CLAUDE.md`, `llms.txt`, `.mcp.json` | Repo-local and local MCP discovery are in place. | Discovery, Context Files |
| Low | repo root | No `llms-full.txt`, `.well-known` API/MCP cards, robots policy, sitemap, or Markdown content negotiation. | Discovery |

Suggested approach: do not add web-only machinery unless Motif gets hosted docs. If docs are published, generate `llms-full.txt`, `.well-known` capability cards, sitemap, and Markdown fallbacks.

Dependencies: hosted docs/site decision.

### 5. Testing Is Strong Deterministically, Missing Agent Eval Fixtures

The deterministic test suite covers the CLI, SDK, MCP protocol, and package smoke paths, but there is no labeled agent-task regression suite.

| Severity | File | Issue | Dimension |
| --- | --- | --- | --- |
| Resolved | `packages/motif-mcp/tests/tools.test.ts` | MCP protocol tests cover annotations, resources, structured errors, and creative pass-through. | MCP Server, Testing |
| Resolved | `apps/cli/tests/cli-contract.test.ts` | CLI contract tests cover describe schema, dry runs, stdin JSON, structured errors, utility tools, and Series creative prompts. | CLI Design, Testing |
| Medium | repo root | No `tests/agent-tasks/*.jsonl` fixture suite for recurring CLI/MCP agent-task regression. | Testing |
| Low | `.github/workflows/ci.yml` | CI runs deterministic tests but no optional eval/canary reporting job. | Testing |

Suggested approach: add a small fixture set of representative agent tasks and expected structured outputs, all using dry-run CLI calls or mocked MCP calls. Keep live fal canaries opt-in.

Dependencies: none.
