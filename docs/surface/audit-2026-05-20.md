> Historical record from before Tasks replaced models ([ADR 0001](../adr/0001-tasks-replace-models.md)). Current commands are in [apps/cli/docs/verbs.md](../../apps/cli/docs/verbs.md).

# Surface Audit: Motif

Date: 2026-05-20

## Detected Stack

- TypeScript pnpm/turbo monorepo with workspace packages in `apps/cli`, `packages/motif-sdk`, `packages/motif-mcp`, and `packages/motif-server`.
- Public package surfaces: `@howells/motif-cli`, `@howells/motif-sdk`, `@howells/motif-mcp`, and deprecated `@howells/motif-server`.
- Runtime surfaces: Node 22 CLI, Node SDK using platform `fetch`, local stdio MCP server, terminal Ink Studio.
- Agent surfaces present: CLI structured output, stdin JSON, `--dry-run`, `--describe`, field masks, MCP tools/resources, package smoke tests, CLI contract tests, root `AGENTS.md`, `llms.txt`, `.mcp.json`, and package-scoped `apps/cli/AGENTS.md`.
- No HTTP API, hosted web app, public OpenAPI spec, remote MCP transport, retrieval/RAG corpus, or multi-agent runtime in this public repo.

## Scorecard

```text
==============================================================================
                           SURFACE SCORECARD
                           Motif
                           2026-05-20
==============================================================================

  1. API Surface          [---]  N/A   No HTTP API surface
  2. CLI Design           [##.]  2/3   Strong JSON/schema/dry-run surface; exit codes are not semantic
  3. MCP Server           [###]  3/3   Stdio MCP with tools, schemas, read-only resources, structured errors, and tests
  4. Discovery & AEO      [##.]  2/3   README, root AGENTS.md, llms.txt, package docs, and local .mcp.json
  5. Authentication       [##.]  2/3   FAL_KEY/envy config plus documented MCP, history, and ephemeral boundaries
  6. Error Handling       [##.]  2/3   Structured CLI errors with doc_uri/suggestions; no trace_id or semantic exit codes
  7. Tool Design          [##.]  2/3   Typed SDK/MCP tools with schemas; descriptions and cross-surface parity need work
  8. Context Files        [###]  3/3   Root AGENTS.md, CLAUDE.md overlay, package AGENTS.md, and package READMEs
  9. Multi-Agent          [---]  N/A   Not an agent orchestration system
  10. Testing             [##.]  2/3   CLI/MCP/package tests and CI; no agent eval dataset or regression metrics
  11. Data Retrievability [---]  N/A   No retrievable knowledge/RAG surface

==============================================================================
  TOTAL: 18/24 (scaled: 23/30)
  RATING: Agent-first

  Human-only        Agent-tolerant      Agent-ready        Agent-first
  0          7      8           14      15        22       23        30
==============================================================================
```

## Evidence

- Monorepo commands and package manager are defined in `package.json:4-16` and `package.json:36`.
- CLI package exposes the `motif` binary and public package metadata in `apps/cli/package.json:2-17`.
- SDK package exposes CJS/ESM/types exports and an allowlisted package surface in `packages/motif-sdk/package.json:21-39`.
- README documents structured agent use, dry runs, stdin JSON, schema introspection, and current options in `README.md:56-66`, `README.md:360-371`, and `README.md:397-455`.
- CLI output format detection, field masks, JSON/NDJSON emission, and structured error output live in `apps/cli/src/utils/output.ts:12-24`, `apps/cli/src/utils/output.ts:78-104`, and `apps/cli/src/utils/output.ts:106-195`.
- CLI input hardening rejects control characters, traversal, encoded traversal, and unsafe output paths in `apps/cli/src/utils/input.ts:12-57` and `apps/cli/src/utils/input.ts:109-145`.
- CLI contract tests cover help, `--describe`, structured errors, dry runs, current fal body normalization, stdin JSON, utility tools, and `--ephemeral` dry runs in `apps/cli/tests/cli-contract.test.ts:72-230`.
- MCP server is extracted for testability in `packages/motif-mcp/src/create-server.ts`, uses stdio transport in `packages/motif-mcp/src/index.ts`, and exposes five tools plus read-only resources.
- MCP tools include annotations, `outputSchema`, “use when” descriptions, and structured success results in `packages/motif-mcp/src/create-server.ts`.
- MCP recoverable failures return structured `isError: true` tool results with code, message, retryability, and suggestions in `packages/motif-mcp/src/create-server.ts`.
- MCP resources expose model metadata, utility tool metadata, leaderboard snapshots, and local history schema without exposing local history values in `packages/motif-mcp/src/create-server.ts`.
- MCP tests use `InMemoryTransport.createLinkedPair()` and verify tool listing, annotations, `outputSchema`, calls, resource listing/reading, and structured error results in `packages/motif-mcp/tests/tools.test.ts`.
- CI runs install, typecheck, lint, and tests in `.github/workflows/ci.yml:24-28`.
- Fal key parsing uses `@howells/envy` in `packages/motif-sdk/src/env.ts:1-21`; SDK requests attach `Authorization: Key ...` in `packages/motif-sdk/src/server.ts:568-587`.
- Root context is now `AGENTS.md`, with `CLAUDE.md` as a thin overlay and `llms.txt` as the agent-readable index.

## Post-Transform Status

Completed:

- Added root `AGENTS.md`, `llms.txt`, `.mcp.json`, package READMEs, and `docs/security.md`.
- Added MCP resources for model registry, fal utility tools, leaderboard snapshots, and history schema.
- Converted recoverable MCP tool failures into structured `isError: true` results.
- Expanded MCP tests for resources and structured failures.

Still open:

- Semantic CLI exit codes and trace IDs remain deliberate contract work.
- Agent task regression fixtures are still missing.

## Findings

### 1. Root Agent Discovery Is Complete For Local Repo Use

The repo now has the universal entrypoints most coding agents look for first.

| Severity | File | Status | Dimension |
| --- | --- | --- | --- |
| Resolved | `AGENTS.md` | Root cross-tool agent instructions added. | Context Files, Discovery |
| Resolved | `CLAUDE.md` | Now a thin overlay that points to root `AGENTS.md`. | Context Files |
| Resolved | `llms.txt` | Agent-readable repo index added. | Discovery |

Remaining opportunity: add `llms-full.txt` or hosted `.well-known` discovery only if Motif gets a documentation site.

Dependencies: none.

### 2. MCP Is Now Agent-First For The Local Stdio Surface

The MCP server now has real protocol tests, resources, structured success outputs, and structured recoverable errors.

| Severity | File | Status | Dimension |
| --- | --- | --- | --- |
| Resolved | `packages/motif-mcp/src/create-server.ts` | Recoverable failures return structured `isError: true` tool results. | MCP Server, Error Handling |
| Resolved | `packages/motif-mcp/src/create-server.ts` | Read-only resources added for models, tools, leaderboards, and history schema. | MCP Server |
| Resolved | `packages/motif-mcp/src/create-server.ts` | Tool descriptions now include use cases and side-effect boundaries. | MCP Server, Tool Design |
| Resolved | `.mcp.json` | Local stdio discovery manifest added. | MCP Server, Discovery |

Remaining opportunity: add prompts only if Motif starts offering reusable prompt workflows rather than raw generation tools.

Dependency decision resolved: local history values remain a tool result gated by an explicit `history` call; the resource exposes schema only.

### 3. CLI Error Semantics Stop Short Of Full Autonomy

The CLI has strong structured error objects, but every terminal error path still collapses to generic process failure semantics.

| Severity | File | Issue | Dimension |
| --- | --- | --- | --- |
| Medium | `apps/cli/src/utils/errors.ts`, `apps/cli/src/utils/output.ts` | Structured error body includes status/doc_uri/suggestions, but process exit codes are not semantic. | CLI Design, Error Handling |
| Medium | `apps/cli/src/utils/output.ts` | No `trace_id` or `instance` field for correlating failed CLI runs. | Error Handling |
| Low | `apps/cli/src/commands/describe.ts` | Error docs are local schema entries, not linked to stable markdown docs. | Error Handling, Discovery |

Suggested approach: map error metadata to sysexits-style exit codes, add generated trace IDs to structured errors, and document error code pages under `docs/errors/`.

Dependencies: decide whether changing exit codes is a breaking CLI contract.

### 4. Public Package Discovery Is Now Agent-Readable

Motif’s README is comprehensive, and the repo now has compact package-level discovery for agents and humans.

| Severity | File | Status | Dimension |
| --- | --- | --- | --- |
| Resolved | `llms.txt` | Compact agent index added. | Discovery & AEO |
| Resolved | `packages/motif-sdk/README.md`, `packages/motif-mcp/README.md`, `packages/motif-server/README.md`, `apps/cli/README.md` | Package-local docs added and included by package `files` allowlists. | API Surface, Discovery |
| Resolved | `README.md` | Compact “Agent Entry Points” section added. | Discovery & AEO |

Remaining opportunity: generate API reference pages from TypeScript declarations if a docs site is added.

Dependencies: none.

### 5. Auth Is Pragmatic And Documented, But Broad

Motif delegates auth to fal’s API key model, which is workable for local tools but not fine-grained. The local trust boundaries are now documented.

| Severity | File | Issue | Dimension |
| --- | --- | --- | --- |
| Resolved | `docs/security.md`, `README.md`, `packages/motif-mcp/README.md` | Auth docs now spell out `FAL_KEY`, local config, MCP trust, history exposure, and `--ephemeral` caveats. | Authentication |
| Low | `packages/motif-mcp/src/index.ts` | MCP server exits with a plain fatal message when `FAL_KEY` is missing. | Authentication, Error Handling |

Suggested approach: keep `docs/security.md` current as fal auth capabilities change, and improve the MCP startup error shape if this becomes a support issue.

Dependencies: fal’s available key/scoping model.

### 6. Testing Is Strong For Deterministic Surfaces, Missing Evals

The deterministic test suite is unusually good for a CLI/MCP package, but there is no eval or trace regression layer.

| Severity | File | Issue | Dimension |
| --- | --- | --- | --- |
| Resolved | `packages/motif-mcp/tests/tools.test.ts` | MCP tests now cover structured `isError` results and resource reads. | MCP Server, Testing |
| Medium | repo root | No small labeled “agent tasks” fixture set for CLI/MCP tool-use regression. | Testing |
| Low | `.github/workflows/ci.yml` | CI runs deterministic tests but no optional eval/canary reporting job. | Testing |

Suggested approach: add a `tests/agent-tasks/*.jsonl` fixture suite that runs dry-run CLI and MCP calls against expected schemas; keep live fal canaries opt-in.

Dependencies: none.
